<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="practicas.cursoandroid.practicasandroid.activity.DialogsActivity">

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Toast"
        android:id="@+id/btn_dialogs_toast"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:onClick="showToast" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Dialog"
        android:id="@+id/btn_dialogs_dialog"
        android:layout_below="@+id/btn_dialogs_toast"
        android:layout_alignStart="@+id/btn_dialogs_toast"
        android:onClick="showDialog" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Progress"
        android:id="@+id/btn_dialogs_progress"
        android:layout_below="@+id/btn_dialogs_dialog"
        android:layout_centerHorizontal="true"
        android:onClick="showProgress" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Date"
        android:id="@+id/btn_dialogs_date"
        android:layout_below="@+id/btn_dialogs_progress"
        android:layout_centerHorizontal="true"
        android:onClick="showDate" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Hour"
        android:id="@+id/btn_dialogs_hour"
        android:layout_below="@+id/btn_dialogs_date"
        android:layout_centerHorizontal="true"
        android:onClick="showHour" />
</RelativeLayout>
